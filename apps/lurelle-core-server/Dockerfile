# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build lurelle-core-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t lurelle-core-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system lurelle-core-server && \
          adduser --system -G lurelle-core-server lurelle-core-server

COPY dist/apps/lurelle-core-server lurelle-core-server
RUN chown -R lurelle-core-server:lurelle-core-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix lurelle-core-server --omit=dev -f install

CMD [ "node", "lurelle-core-server" ]
